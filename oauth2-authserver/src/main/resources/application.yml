server:
  port: 9999
  contextPath: /uaa
security:
  user:
    password: password
logging:
  level:
    org.springframework.security: DEBUG

app:
  security:
    keystore:
      file: keystore.jks
      password: foobar
      key: test